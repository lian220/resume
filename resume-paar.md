<!-- 자동 생성 파일 — 직접 수정 금지. base/resume-data.mjs 수정 후 `npm run build`. -->
# 임도영 (Lian) — 백엔드 시니어 엔지니어 (PAAR 포맷)

📧 lian.dy220@gmail.com ｜ 💻 github.com/lian220 ｜ 📦 github.com/lian220/quant-jump-stock-portfolio

> **자기소개** — 소셜 로그인으로 신규 가입을 **일 10명 → 150명(15배)**으로 늘리고, **결제·환불·리워드 회귀 0건**의 멤버십 빌링을 BE 단독 설계한 **7년 차 백엔드 엔지니어**. 구현보다 **"왜 이 설계를 골랐나(트레이드오프)"**를 드러내고, 관측성·장애 대응으로 운영 안정성을 끝까지 책임집니다.

---

## 1. 사용자 인증·인가 시스템 풀스택 (SNS 로그인 + RBAC)

| | |
|---|---|
| 🟥 **P** | 차별화상회는 ID/PW 6단계 가입만 있어 모바일 가입 마찰이 최대 이탈 병목. ROS 백오피스는 권한이 코드 곳곳에 흩어져 본부·점주·관리자 통제 불가. |
| 🟨 **A**(분석) | 토큰 — 외부 IdP 종속(SDK 결합) vs 자체 발급: 토큰 수명·강제 무효화를 직접 통제하려 **자체 JWT** 선택 (Implicit Grant 배제, OAuth 2.1 방향) · 권한 — JWT에 권한 전부 적재(stale·위조 위험) vs 게이트웨이 중앙집행: **중앙 집행 + 서버사이드 이중검증** 선택, JWT 권한은 FE 분기 전용으로 한정 |
| 🟦 **A**(실행) | OAuth 2.1 PKCE(S256) → 자체 JWT + **Refresh Token Rotation** — 구 토큰 재사용 시 세션 전체 무효화로 replay 방어 · RBAC **3단 모델(ROOT/FUNCTION/ENDPOINT) + 객체레벨 인가**로 수직 권한 상승 차단(OWASP API1/3), 게이트웨이 중앙 RBAC + Redis 분산 캐시 + 즉시 무효화 |
| 🟩 **R** | 신규 가입 **일 10명 → 150명 (15배 / +1,400%)** / 준회원 → 정회원 **전환율 30.2%** (9,723건 중 2,936건) / 인증 관련 운영 장애 **0건**, 다운스트림 6서비스 JWT 재파싱 제거 |

---

## 2. 멤버십 자동결제(토스 빌링) + 추천인 그로스 루프

| | |
|---|---|
| 🟥 **P** | 카드 1회 등록 → 매월 자동결제 + 가입·연체·해지 라이프사이클이 필요. 돈이 걸린 흐름이라 중복 결제·이중 지급 = 즉시 사고. |
| 🟨 **A**(분석) | 카드 정보 — 자체 보관(PCI DSS 스코프 부담) vs 빌링키 위임: **빌링키만 보관(vault 위임)**으로 카드데이터 스코프 자체를 제거 · 중복 방지 — 분산락(인프라·운영 추가) vs DB 멱등성: **DB 유니크 제약 + 조건부 UPDATE**로 외부 락 없이 정확히 1회 |
| 🟦 **A**(실행) | 토스 빌링키 + idempotency 키(memberId+billingDate)로 배치 재실행 시 중복 결제 차단, API/DB 트랜잭션 경계 분리로 외부 지연에도 일관성 · exponential backoff 재시도 → N회 실패 시 연체 전환, 추천 리워드 **D+8 멱등 지급(PENDING→GRANTED, 정확히 1회)** + 환불 시 제외 · 돈이 걸린 흐름은 **blind auto-retry 대신 실패 격리 + Slack 알림 + 수동 멱등 재처리** |
| 🟩 **R** | 멤버십 자동결제 **BE 단독 출시** — 카드 1회 등록으로 매월 자동결제, 운영 개입 없이 안정 운영 / 결제·환불·리워드 회수 흐름 **회귀 0건** — 결제 도메인 안정성 = 매출 안정성 / *[확인 필요: 카드 등록 회원 결제 성공률 N%, 추천코드 경유 신규 가입 N%]* |

---

## 3. 본부↔점주 양방향 소통관리 도메인 신규 구축 (외식UP ROS)

| | |
|---|---|
| 🟥 **P** | ROS 1.0 출시에 본부-점주 채널이 부재 → 공지 미전달·동의서 누락·공문 확인 불가 리스크. 공지/동의서/공문 3종 + 알림톡 + 1:1 문의가 묶인 도메인 필요. |
| 🟨 **A**(분석) | 모델 — 타입별 테이블 분리 vs 공통 라이프사이클 공유: **STI(단일 테이블 상속)**로 폴리모픽 목록 조회 최적화 (타입별 nullable 수용) · 알림톡 정합성 — 단순 retry(dual-write 유실) vs 트랜잭션 분리: **Transactional Outbox**로 게시글 커밋과 발송요청을 분리 처리 |
| 🟦 **A**(실행) | 본부향 CRUD(임시저장/발행) + 점주향 동의·확인 흐름 + 카카오 알림톡 연동을 BE 단독 구축 · 출시 후 실행계획 분석 → **N+1 제거 + 복합 인덱스 + Fetch Join**으로 목록/상세 성능 개선 |
| 🟩 **R** | 제로에서 **39건 API BE 단독 구축** (본부향 26 + 점주향 13, 전건 배포완료) / QA 이슈 **14건 전건 종결** (첨부/업로드·권한·알림톡·연동 안정화) / *[확인 필요: 구축 기간 N개월, 목록 P95 N→Mms 개선]* |

---

## 4. 관측성(Observability) 자발 도입 → 팀 표준 정착

| | |
|---|---|
| 🟥 **P** | 운영 장애를 로그 grep으로만 추적 → 디버깅 병목. 팀 관측성 표준 자체가 부재. 대안인 SaaS(Datadog/New Relic)와 AWS 네이티브(X-Ray + CloudWatch) 모두 트레이스·로그 인제스트가 사용량 비례 변동비라 트래픽 증가 시 비용 예측이 어렵고, SaaS는 사용자/매출 데이터 외부 반출 우려까지 존재. |
| 🟨 **A**(분석) | SaaS(호스트·볼륨 과금) · AWS 네이티브(X-Ray + CloudWatch Logs 인제스트 GB 과금)가 모두 **사용량 비례 변동비**임을 확인 → **기존 운영 EC2의 유휴 리소스에 얹어 추가 인스턴스 비용 0(증분 = EBS 디스크뿐)**으로 도입, 트래픽·로그량이 늘어도 비용이 선형 증가하지 않는 구조라 판단해 SigNoz 채택을 직접 설득 · 단일 UI·trace-log 상관은 AWS Application Signals로도 가능하나, **OTel-native = 백엔드 이식이 자유로워 X-Ray 종속(벤더락인)을 회피**하는 점을 차별점으로 판단 · "관측성 다운 ≠ 서비스 다운"이라 **HA는 과투자**로 판단 — 단일 인스턴스가 합리적 |
| 🟦 **A**(실행) | **기존 운영 EC2에 docker-compose로 추가 배포**(신규 인스턴스 0, 단일 노드), EBS 스냅샷 백업 + ClickHouse TTL 보존 · OTel Java Agent 자동 계측 + 핵심 비즈니스 흐름만 **커스텀 span 선별**(폭증 방지) · traceId를 **Logback MDC 주입**해 로그 ↔ 분산 Trace 연관, API별 에러율·P95 대시보드 + Slack 알림 |
| 🟩 **R** | 로그 grep 의존 → **분산 트레이스 단일 조회로 근본원인 추적** / **데이터 주권 확보** (사용자/매출 trace 사내 인프라 유지), 팀 표준 정착 / *[확인 필요: **SaaS·AWS 네이티브(사용량 비례 변동비) 대비 추가 인스턴스 0(기존 EC2 재활용)**으로 연 N만원 절감, 디버깅 N시간 → M분 단축, 확산 N명/N개 서비스]* |

---

<sub>※ 이전 경력은 resume-career.html, 상세·면접 답변은 resume-star.md 참조. (base/resume-data.mjs 에서 생성)</sub>
